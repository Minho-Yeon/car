마지막 open()이 이루어지기 바로 전 root 값들을 받아옴
그 중중 bottom과 연결되 있는 것들 중
마지막 open()이 이루어지고 full된 것들을 제외한 모든 것들은 false를 리턴하도록 함

1. 배열을 만들어 wuf의 배열과 같은 값을 가지도록 만듦

2. 배열 두개 만들어 마지막 open 전의 배열과 그 후의 배열의 값을 만들어 같은 다른 것만 true;